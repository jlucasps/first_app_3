

<% if resource.errors.any? %>
  <div class="alert alert-danger">

    <h2>
      <%= pluralize(resource.errors.count, "Erro", "Erros") %>
    </h2>
    <ul>
      <% resource.errors.full_messages.each do |msg| %>   
        <li><%= msg %></li>
      <% end %>
    </ul>

  </div>
<% end %>